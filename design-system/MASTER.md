# Design System — Master

> This file is the **global source of truth** for design tokens, color systems, typography,
> spacing, animation, and chart standards across all features built by this engineering team.
>
> Generated by `/agent-design` (ux-designer persona). Updated each time a feature introduces
> new patterns. Feature-specific overrides live in `design-system/pages/<feature-name>.md`.
>
> **The `frontend-engineer` reads this before writing a single component.**

---

## Status

Awaiting first `/agent-design` run. This file will be populated with:

- Industry classification + design rationale
- Complete color token system (semantic roles, not raw hex)
- Typography pairings (UI font + data/monospace font)
- Spacing scale
- Border radius and shadow system
- Animation tokens and motion rules
- Z-index scale
- Icon system rules
- Chart type palette and library decisions
- Pre-delivery checklist baseline

Run `/agent-design` on your first feature to generate the full system. The ux-designer will
classify your product type and apply the industry-appropriate design rules automatically.

---

## Quick reference — Financial / Trading Dashboard defaults

If you're building a trading or financial dashboard and need to start immediately:

```css
/* Color tokens */
--color-bg-base:        #0A0E17;
--color-bg-surface:     #141B2D;
--color-bg-elevated:    #1A2540;
--color-border:         #1E2D40;
--color-text-primary:   #E8EAF0;
--color-text-secondary: #8892A4;
--color-brand:          #2196F3;
--color-signal-buy:     #26A69A;
--color-signal-sell:    #EF5350;
--color-signal-hold:    #FFA726;

/* Typography */
--font-data: 'JetBrains Mono', monospace;   /* numbers, prices, % */
--font-ui:   'Inter', sans-serif;           /* labels, nav, headings */

/* Animation */
--duration-fast:   150ms;
--duration-base:   200ms;
--duration-slow:   300ms;
--easing-out:      cubic-bezier(0.0, 0.0, 0.2, 1);
```

These are defaults only — run `/agent-design` to get the full reasoned system for your product.
